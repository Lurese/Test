 
 // 4 Print a table containing multiplication tables
for(let i = 1; i<= 12; i++) {
    let row= '';
    for(let j = 1; j <= 20; j++) {
        row += (i * j).toString().padStart( 4, '  ') + '  '
    }
    console.log(row);
} 